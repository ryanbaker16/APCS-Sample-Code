/** * Coords.java (Direction) * Code Description: Draw rectangles to illustrate the Java coordinate system * @author ppatel * @version 10/9/09   9/15/11 * @contact mrpatelis@gmail.com */        import java.applet.Applet;import java.awt.*;public class Coords extends Applet{	/**	 * main method -      * @param page	 */	public void paint (Graphics page)	{		//Declare size constants		final int MAX_SIZE = 300;		final int PAGE_WIDTH = 600;		final int PAGE_HEIGHT = 400;		// Declare variables		int x, y;    // x and y coordinates of upper left-corner of each shape		int width, height; // width and height of each shape 		// Set the background color		setBackground (Color.gray);		// Set the color for the next shape to be drawn		page.setColor (Color.blue);		// Assign the corner point and width and height		width = 150;		height = 100;		x = PAGE_WIDTH/2-width/2;		y = PAGE_HEIGHT/2-height/2;				page.fillRect(x, y, width, height);				page.setColor(Color.red);		width = 100;		height = 100;		page.fillRect(0, 0, width, height);		page.setColor(Color.pink);		page.fillRect(PAGE_WIDTH-width, 0, width, height);		page.setColor(Color.cyan);		page.fillRect(0, PAGE_HEIGHT-height, width, height);		page.setColor(Color.magenta);		page.fillRect(PAGE_WIDTH-width, PAGE_HEIGHT-height, width, height);    }}